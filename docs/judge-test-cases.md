# Completeness-judge test cases → moved

Everything that was here now lives in `docs/judge_test_cases.md` (the file the code, the tests and the
`replay-traces` skill all point at), under "Hand-written expectation cases" — the eight scenarios, the
2026-06-28 verification and its trace ids included. This file is a leftover of a near-duplicate name
and can be deleted.
