"""REMOVED — the tool registry moved to `app/tools/` (generic `ToolRegistry` + `app/tools/wiring.py`).

This file is a tombstone: nothing imports it. Delete it with `git rm app/subagents/registry.py`
(the harness can't run `git rm`, so it's left here for the owner to remove).
"""
